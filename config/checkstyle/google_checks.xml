<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <property name="charset" value="UTF-8"/>

    <!-- Configuración básica para empezar -->
    <module name="TreeWalker">

        <!-- Verifica que el nombre del archivo coincida con la clase pública -->
        <module name="OuterTypeFilename"/>

        <!-- Verifica imports no utilizados -->
        <module name="UnusedImports"/>

        <!-- Verifica imports redundantes -->
        <module name="RedundantImport"/>

        <!-- Longitud máxima de métodos -->
        <module name="MethodLength">
            <property name="max" value="50"/>
            <property name="tokens" value="METHOD_DEF"/>
        </module>

        <!-- Longitud máxima de parámetros -->
        <module name="ParameterNumber">
            <property name="max" value="7"/>
        </module>

    </module>

    <!-- Longitud máxima del archivo -->
    <module name="FileLength">
        <property name="max" value="2000"/>
    </module>

    <!-- Verifica que no haya tabulaciones -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>
</module>